# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CleanTableCollectionView' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CleanTableCollectionView
    pod 'DataSourceKit', '0.2.0', :inhibit_warnings => true
    pod 'AlamofireImage', '~> 3.4'
    pod 'Firebase/Core'
    pod 'Fabric'
    pod 'Crashlytics'

  target 'CleanTableCollectionViewTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CleanTableCollectionViewUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
